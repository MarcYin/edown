# CLI

## `edown search`

Discovers matching images and writes a manifest without downloading raster data.

## `edown download`

Runs discovery and downloads native-grid GeoTIFFs in parallel.

## `edown stack`

Reads a manifest and builds one Zarr store per compatible alignment group.

Use `edown --help` and `edown <command> --help` for the full option set.
